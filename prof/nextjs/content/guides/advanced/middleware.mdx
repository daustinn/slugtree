---
title: Edge Middleware
description: Running documentation checks and redirects at the edge.
icon: zap
---

# Edge Middleware

Integrate Slugtree helper functions with Next.js Edge Middleware for authentication or redirects.

## Example

Use `getSlugs()` or `isNodeActive()` inside `middleware.ts` to validate route access.
